Framework for plain text to java binding using expressive grammar(BNF+regexps) to specify binding rules. GLR parser with extensible disambiguation logic.

Btw, this is **university research project**:)